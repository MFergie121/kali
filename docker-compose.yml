services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: kali-afl
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5435:5432"
    volumes:
      - kali-afl-db:/var/lib/postgresql/data

volumes:
  kali-afl-db:
